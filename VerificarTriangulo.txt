    setlocale(LC_ALL,"Portuguese");
    int a,b,c;
    scanf("%d%d%d", &a, &b, &c);
    if(a<b+c && b<a+c &&c<a+b){
        printf("Existe um triângulo");
        if (a!=b && b!=c && c!=a)
            printf(" escaleno.\n");
        else if (a==b && b==c)
            printf(" equilátero.\n");
        else
            printf(" isóceles.\n");
}
    else
        printf("Não existe triângulo.\n");